import React from 'react';
import Navigation from './Navigation';

const App = () => {
  // Firebase auto-initializes from google-services.json
  // No manual initialization needed - React Native Firebase handles it automatically
  return <Navigation />;
};

export default App;
